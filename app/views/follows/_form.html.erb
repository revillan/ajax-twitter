<button type="button" class="follow-toggle" data-user-id="<%= user.id %>"
   data-initial-follow-state="<%= current_user.follows?(user) ? 'followed' : 'unfollowed'%>" name="button"></button>

  <!-- <%= button_to "Unfollow", user_follow_url(user), method: :delete %> -->

  <!-- <%= button_to "Follow", user_follow_url(user), method: :post %> -->
